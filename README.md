# KursPy - Визуализация алгоритмов

Проект для визуализации и работы с различными алгоритмами: графы, матрицы и сортировка. Включает графический интерфейс на Tkinter и базу данных SQLite для сохранения результатов.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Требования](#требования)
- [Установка](#установка)
- [Использование](#использование)
- [Структура проекта](#структура-проекта)
- [Описание модулей](#описание-модулей)
- [Тестирование](#тестирование)
- [База данных](#база-данных)

## 📖 Описание

KursPy - это образовательное приложение для изучения и визуализации алгоритмов. Проект включает три основных модуля:

1. **Графы** - алгоритм Флойда-Уоршелла для поиска кратчайших путей
2. **Матрицы** - умножение матриц (квадратных и прямоугольных)
3. **Сортировка** - сравнение алгоритмов сортировки

## ✨ Возможности

### Графы
- Интерактивное рисование графов
- Добавление вершин и рёбер с весами
- Генерация случайных графов
- Визуализация графа на canvas
- Алгоритм Флойда-Уоршелла для поиска кратчайших путей
- Сохранение и загрузка графов из базы данных

### Матрицы
- Генерация случайных матриц (квадратных и прямоугольных)
- Редактирование матриц через удобный интерфейс
- Умножение матриц с проверкой совместимости размеров
- Отображение результатов с метриками производительности
- Сохранение и загрузка матриц из базы данных

### Сортировка
- Два алгоритма сортировки:
  - Пузырьковая сортировка (O(n²))
  - Сортировка выбором (O(n²))
- Генерация случайных массивов с настройкой размера и диапазона
- Сравнение алгоритмов по времени выполнения и количеству сравнений
- Сохранение результатов в базу данных

### База данных
- Автоматическое создание SQLite базы данных
- Сохранение графов, матриц и результатов сортировки
- Загрузка сохраненных данных
- История всех операций

## 🔧 Требования

- Python 3.9+ (рекомендуется Python 3.9.6 или выше)
- Tkinter (обычно входит в стандартную установку Python)
- SQLite3 (встроен в Python)

**Важно для macOS:** Если используете Homebrew Python, убедитесь, что он скомпилирован с поддержкой Tkinter. В противном случае используйте системный Python.

## 📦 Установка

1. Клонируйте репозиторий или скачайте проект:
```bash
cd /path/to/KursPy
```

2. (Опционально) Создайте виртуальное окружение:
```bash
python3 -m venv .venv
source .venv/bin/activate  # На macOS/Linux
# или
.venv\Scripts\activate  # На Windows
```

3. Убедитесь, что все файлы на месте:
```
KursPy/
├── main.py          # Главный файл приложения
├── graph.py         # Алгоритм Флойда-Уоршелла
├── matrix.py        # Умножение матриц
├── sort.py          # Алгоритмы сортировки
├── database.py      # Работа с БД
└── test_all.py      # Тесты
```

## 🚀 Использование

### Запуск приложения

```bash
python3 main.py
```

### Основные функции

#### Вкладка "Графы"

1. **Добавление вершин**: Выберите режим "Добавить вершину" и кликните на canvas
2. **Добавление рёбер**: Выберите режим "Добавить ребро", кликните на две вершины, введите вес
3. **Удаление**: Выберите режим "Удалить" и кликните на вершину
4. **Генерация случайного графа**: Укажите количество вершин и рёбер, нажмите "Сгенерировать случайный граф"
5. **Запуск алгоритма**: Нажмите "Запустить алгоритм" для выполнения алгоритма Флойда-Уоршелла
6. **Сохранение/Загрузка**: Используйте кнопки "Сохранить граф" и "Загрузить граф"

#### Вкладка "Матрицы"

1. **Генерация матриц**: Укажите размеры матриц A и B, нажмите "Сгенерировать матрицы"
2. **Редактирование**: Используйте кнопки "Редактировать матрицу A/B" для ручного ввода
3. **Умножение**: Нажмите "Умножить матрицы" для вычисления результата
4. **Сохранение/Загрузка**: Используйте кнопки "Сохранить матрицы" и "Загрузить матрицы"

#### Вкладка "Сортировка"

1. **Генерация массива**: Укажите размер и диапазон значений, нажмите "Сгенерировать массив"
2. **Редактирование**: Используйте "Редактировать массив" для ручного ввода
3. **Сравнение алгоритмов**: Нажмите "Запустить сравнение" для сравнения всех алгоритмов
4. **Сохранение результатов**: Используйте "Сохранить результат" после выполнения сравнения

## 📁 Структура проекта

```
KursPy/
├── main.py              # Главное приложение с GUI
├── graph.py             # Алгоритм Флойда-Уоршелла
├── matrix.py            # Умножение матриц и генерация
├── sort.py              # Алгоритмы сортировки
├── database.py          # Работа с SQLite базой данных
├── test_all.py          # Все тесты проекта
├── kurspy.db            # База данных SQLite (создается автоматически)
└── README.md            # Этот файл
```

## 📚 Описание модулей

### `graph.py`
- **`floyd_warshall(graph)`** - Алгоритм Флойда-Уоршелла для поиска кратчайших путей между всеми парами вершин
  - Возвращает: матрицу расстояний, количество сравнений, время выполнения

### `matrix.py`
- **`matrix_multiplication(A, B)`** - Умножение матриц A и B
  - Поддерживает квадратные и прямоугольные матрицы
  - Возвращает: результирующую матрицу, количество сравнений, время выполнения
- **`generate_matrix(rows, cols=None)`** - Генерация случайной матрицы
  - Если `cols` не указан, создается квадратная матрица

### `sort.py`
- **`bubble_sort(arr)`** - Сортировка пузырьком O(n²)
- **`selection_sort(arr)`** - Сортировка выбором O(n²)
- **`compare_sorts(arr)`** - Сравнение всех алгоритмов сортировки
- **`generate_test_data(size=100)`** - Генерация тестовых данных

### `database.py`
- **`Database`** - Класс для работы с SQLite базой данных
  - `save_graph()` - Сохранение графа
  - `get_all_graphs()` - Получение всех графов
  - `save_matrices()` - Сохранение матриц
  - `get_all_matrices()` - Получение всех матриц
  - `save_sort_result()` - Сохранение результата сортировки
  - `get_all_sorts()` - Получение всех результатов сортировки

## 🧪 Тестирование

Запуск всех тестов:

```bash
python3 test_all.py
```

Или через unittest:

```bash
python3 -m unittest test_all.py -v
```

### Покрытие тестами

- ✅ Алгоритм Флойда-Уоршелла (7 тестов)
- ✅ Умножение матриц (8 тестов)
- ✅ Генерация матриц (5 тестов)
- ✅ Сортировка пузырьком (7 тестов)
- ✅ Сортировка выбором (7 тестов)
- ✅ Сравнение алгоритмов (3 теста)
- ✅ Генерация тестовых данных (4 теста)
- ✅ Согласованность алгоритмов (1 тест)

**Всего: 41 тест**

## 💾 База данных

База данных SQLite создается автоматически при первом запуске в файле `kurspy.db`.

### Таблицы:

1. **`graphs`** - Сохраненные графы
   - id, name, vertices, edges, matrix, created_at

2. **`matrices`** - Сохраненные матрицы
   - id, name, matrix_a, matrix_b, result, created_at

3. **`sorts`** - Результаты сортировки
   - id, name, array_size, input_array, algorithm, sorted_array, comparisons, time_taken, created_at

## 🐛 Решение проблем

### Проблема: "ModuleNotFoundError: No module named '_tkinter'"

**Решение:**
1. Убедитесь, что используете Python с поддержкой Tkinter
2. На macOS: используйте системный Python (`/usr/bin/python3`) вместо Homebrew Python
3. Пересоздайте виртуальное окружение:
```bash
rm -rf .venv
/usr/bin/python3 -m venv .venv
source .venv/bin/activate
```

### Проблема: Ошибки при импорте модулей

**Решение:**
Убедитесь, что все файлы находятся в одной директории и вы запускаете скрипт из корневой папки проекта.

### Проблема: База данных не создается

**Решение:**
Проверьте права доступа к папке проекта. База данных создается автоматически при первом использовании.

## 📝 Примеры использования

### Пример 1: Работа с графом

```python
from graph import floyd_warshall

# Создаем граф (матрица смежности)
graph = [
    [0, 1, 4],
    [1, 0, 2],
    [4, 2, 0]
]

# Находим кратчайшие пути
dist_matrix, comparisons, time_taken = floyd_warshall(graph)
print(f"Кратчайшее расстояние от 0 до 2: {dist_matrix[0][2]}")
```

### Пример 2: Умножение матриц

```python
from matrix import matrix_multiplication, generate_matrix

# Генерируем матрицы
A = generate_matrix(3, 4)  # 3x4
B = generate_matrix(4, 2)  # 4x2

# Умножаем
result, comparisons, time = matrix_multiplication(A, B)
print(f"Результат: {len(result)}x{len(result[0])}")
```

### Пример 3: Сортировка

```python
from sort import compare_sorts

# Сравниваем алгоритмы
arr = [3, 1, 4, 1, 5, 9, 2, 6]
results = compare_sorts(arr)

for name, data in results.items():
    print(f"{name}: {data['time']:.6f} сек, {data['comparisons']} сравнений")
```

## 👤 Автор

Mattt147

Проект создан в рамках курсовой работы по алгоритмам.

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🔄 Версия

Версия 1.0

---

**Примечание:** Проект использует только стандартную библиотеку Python, дополнительные зависимости не требуются.

